#include <stdio.h>

int main(void)
{
    int x = 10;

    x += 5;   // x = x + 5
    x *= 2;   // x = x * 2
    x -= 3;   // x = x - 3

    printf("Final value: %d\n", x);

    return 0;
}
